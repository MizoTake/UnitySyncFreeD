# OutputInspectorSample

この sample は packet preview と diagnostics 表示の確認用です。

1. sample を import します。
2. `Scenes/OutputInspectorSample.unity` を開くか、`BasicVirtualCamera` を複製して利用します。
3. `SyncFreeDPacketPreviewBehaviour` と `SyncDiagnosticsBehaviour` の表示を確認します。
